# TODO - TimeBalance (Navbar & Logo)

- [x] Edit `index.html`: tambah favicon di `<head>`.
- [x] Edit `index.html`: ubah markup logo navbar agar wrap kontainer flex (logo + teks sejajar).
- [x] Edit `index.html`: perbaiki ukuran logo menjadi 45–50px (target: 48px) dan hapus inline height/margin.
- [x] Edit `index.html`: tambahkan CSS responsif untuk logo navbar (mobile).
- [x] Tampilkan kode sebelum & sesudah untuk bagian yang diubah (head favicon + navbar/logo + CSS terkait).
- [ ] Verifikasi layout manual (desktop & mobile breakpoints 900px/600px).
